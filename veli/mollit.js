// Assuming polygonStart is a variable or property
let polygonStart = someValue; // Initialize polygonStart with some value

// Example usage in an object
const polygonObject = {
    polygonStart: polygonStart, // Assigning polygonStart to a property
    // Other properties and methods here
};

// Example usage in a function
function drawPolygon(polygonStart) {
    // Function logic using polygonStart
    console.log('Starting point of polygon:', polygonStart);
}

// Calling the function with polygonStart
drawPolygon(polygonStart);
